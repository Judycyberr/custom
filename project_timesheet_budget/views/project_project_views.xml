<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="project_project_form_view" model="ir.ui.view">
        <field name="name">Project</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='tag_ids']" position="after">
                <field name="budget" string="Budget"/>
                <field name="total_exp" string="Total expense"/>
            </xpath>
        </field>
    </record>

    <record id="project_project" model="ir.actions.act_window">
        <field name="name">Project</field>
        <field name="res_model">project.project</field>
        <field name="views">form,list</field>
        <field name="view_mode">list,kanban,form</field>
    </record>
</odoo>